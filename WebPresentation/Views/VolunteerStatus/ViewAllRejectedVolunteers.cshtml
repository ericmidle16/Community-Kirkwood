<!--
///<summary>
/// Creator: Syler Bushlack
/// Created: 2025-04-28
/// Summary: View for all rejected volunteers

/// Last Updated By: Skyann Heintz
/// Last Updated: 2025-04-28
/// What Was Changed: Styling added.

/// Last Updated By: Dat Tran
/// Last Updated: 2025-04-27
/// What Was Changed: Added Sweet Alerts for changing volunteer statuses. 
/// </summary>
-->
@model IEnumerable<DataDomain.VMVolunteerStatus>

@{
    ViewData["Title"] = "ViewAllRejectedVolunteers";
    int projectid = ViewBag.projectid;
}

<h1 class="text-center">Rejected Volunteers</h1>
@if(TempData["StatusUpdate"] != null)
{
    <script>
        document.addEventListener("DOMContentLoaded", function () {

            if(!sessionStorage.getItem('sweetAlertAcceptVolunteerShown')){
            Swal.fire({
                icon: 'success',
                title: 'Done',
                text: '@TempData["StatusUpdate"]'
                });
                sessionStorage.setItem('sweetAlertAcceptVolunteerShown', 'true');
            }
        });
    </script>
    @* Remove the message so it doesn’t persist on refresh *@
    TempData.Remove("StatusUpdate");

}
else
{
    <script>
        // Ensure the flag is reset if there's no success message
        sessionStorage.removeItem('sweetAlertAcceptVolunteerShown');
    </script>
}
<p>
    <a href="@Url.Action("Index", new { id = projectid })" class="btn btn-info">Volunteer Requests</a>
</p>
<table class="table table-striped table-bordered table-hover">
    <thead class="thead-dark">
        <tr>
            <th class="header">
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th class="header">
                @Html.DisplayNameFor(model => model.Approved)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a href="@Url.Action("UpdateVolunteerStatus", new { approved = true, userID = item.UserID, projectID = item.ProjectID })" class="btn btn-success btn-sm">Accept</a>
            </td>
        </tr>
}
    </tbody>
</table>
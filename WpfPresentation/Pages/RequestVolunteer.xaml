<!--
    <Summary>
    Creator: Akoi Kollie
    Created: 2025/03/19
    Summary: Xaml page for the making a request of volunteers. This UI allows user to enter
    required information about the volunteer, such as name, Content, TimeFrame, and Important
    and submit the form to make request of volunteer.
    Last Updated By: Syler Bushlack
    Last Updated: 2025/05/01
    What was changed: Changed the UI to match the other pages
    </Summary>
-->
<Page x:Class="WpfPresentation.Pages.RequestVolunteer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfPresentation.Pages" xmlns:sys="clr-namespace:System;assembly=mscorlib"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="700"
      Title="Request Volunteer">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>

        </Grid.RowDefinitions>
        <Label x:Name="lblTitle" Grid.Row="0" Content="Request Volunteers" HorizontalAlignment="Center" Style="{StaticResource TitleLabel}"/>


        <Grid Margin="10" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Time Frame -->
            <StackPanel Grid.Column="1" Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Left">
                <Label Content="TimeFrame:" FontSize="10" FontWeight="Bold" Margin="0,0,0,0" Style="{StaticResource LabelTop}"/>
                <DatePicker x:Name="txtTimeFrame"  Grid.Row="3" Width="110" Height="30" VerticalAlignment="Center" Margin="5, 11, 0, 10" DisplayDateStart="{x:Static sys:DateTime.Now}" Style="{StaticResource DateUnder}"/>
            </StackPanel>

            <!-- Number of Volunteers -->
            <StackPanel Grid.Column="0" Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                <Label Content="Name:" FontSize="10" FontWeight="Bold" Margin="0,5,0,0" Style="{StaticResource LabelTop}"/>
                <TextBox x:Name="txtName" Width="110" Height="30" BorderBrush="Gray" BorderThickness="1" Padding="4" />
            </StackPanel>

            <!-- Location -->
            <StackPanel Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <CheckBox x:Name="IsChecked" Width="110" Height="30" Margin="20, 0, 0, 2" Content="Important"/>
            </StackPanel>

            <!-- Project Name -->
            <StackPanel Grid.ColumnSpan="2" Grid.Row="1" Margin="5">
                <Label Content="Content:" FontSize="10" FontWeight="Bold" Margin="0,0,0,2" Style="{StaticResource LabelTop}"/>
                <TextBox Grid.Row="3" x:Name="txtContent" Height="65" TextWrapping="Wrap" BorderBrush="Gray" BorderThickness="1" Padding="4" Style="{StaticResource Description}"/>
            </StackPanel>

            <!-- Skills -->


            <Button x:Name="btnBack" Grid.Row="2" Grid.Column="1" Content="Back" Style="{StaticResource BackButton}" Click="btnBack_Click" />
            <Button x:Name="btnSubmit" Grid.Row="4" Grid.Column="0" Content="Send Request" Style="{StaticResource CallToAction}" Click="btnSubmit_Click"/>
        </Grid>
    </Grid>
</Page>

<!--  
<summary>
Creator:  Josh Nicholson
Created:  2025/02/14
Summary:  Xaml code to create the updateSkills UI structure
</summary>
    
Last Updated By: Stan Anderson
Last Updated: 2025/04/08
-->
<Page x:Class="WpfPresentation.Pages.updateSkills"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfPresentation.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="700"
      Title="updateSkills">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Content="Skills" Style="{StaticResource TitleLabel}"/>

        <Label Grid.Row="1" Grid.Column="1" x:Name="lblSelectSkill" Style="{StaticResource LabelTop}" FontWeight="Bold" Content="Inactive Skills: " />
        
        <DataGrid x:Name="grdInactiveSkillList" Grid.Row="2" Grid.Column="1" SelectionMode="Extended" SelectionUnit="FullRow" IsReadOnly="True" Margin="0,0,10,0"
            ColumnWidth="SizeToCells" AutoGenerateColumns="False" ItemsSource="{Binding}" Height="Auto" GotFocus="grdInactiveSkillList_GotFocus">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Skill Name" Binding="{Binding SkillID}" Width="100" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Label Grid.Row="1" Grid.Column="2" x:Name="lblCurrentSkill" Content="Current Skills: " Style="{StaticResource LabelTop}"/>
        <DataGrid x:Name="grdActiveSkillList" Grid.Row="2" Grid.Column="2" SelectionMode="Extended" SelectionUnit="FullRow" IsReadOnly="True" Margin="10,0,0,0"
    ColumnWidth="SizeToCells" AutoGenerateColumns="False" ItemsSource="{Binding}" Height="Auto" GotFocus="grdActiveSkillList_GotFocus">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Skill Name" Binding="{Binding SkillID}" Width="100" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3" Grid.Column="1" x:Name="btnUserSkillAdd" Content="Add Skill" Click="btnUserSkillAdd_Click" Style="{StaticResource CallToAction}"/>
        <Button Grid.Row="3" Grid.Column="2" x:Name="btnCreateSkill" Content="Create New Skill" Style="{StaticResource CallToAction}" Click="btnCreateSkill_Click"/>
        <Button Grid.Row="4" Grid.Column="1" x:Name="btnUserSkillRemove" Content="Remove Skill" Click="btnUserSkillRemove_Click" Style="{StaticResource DangerButton}"/>
        <Button Grid.Row="4" Grid.Column="2" x:Name="btnBack" Content="Back" Style="{StaticResource BackButton}" Click="btnBack_Click"/>

    </Grid>
</Page>

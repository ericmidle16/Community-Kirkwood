<!--
    /// <summary>
    /// Creator:  Stan Anderson
    /// Created:  2025/02/12
    /// Summary:  This is the page with the list of tasks assigned to an event.
    /// FrameLoad here.
    /// Last Updated By: Stan Anderson
    /// Last Updated: 2025/04/06
    /// What was Changed: Updated UI
    /// </summary>
-->

<Page x:Class="WpfPresentation.Pages.ViewTasksByEventID"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfPresentation.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="700"
      Title="Tasks For Event">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="6*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4">
            <Label x:Name="lblTasksByEventID" Content="Tasks" Style="{StaticResource TitleLabel}"/>
        </Grid>

        <Grid Grid.Row="1"  Grid.Column="1" Grid.ColumnSpan="4">
            <DataGrid x:Name="gridTasksByEventID" IsReadOnly="True" SelectionMode="Single" Loaded="gridTasksByEventID_Loaded"></DataGrid>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="1">
            <Button x:Name="btnViewTask" Style="{StaticResource CallToActionSecondary}" Click="btnViewTask_Click">View Task</Button>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="2">
            <Button x:Name="btnCreateTask" Style="{StaticResource CallToAction}" Click="btnCreateTask_Click">Create New Task</Button>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="3">
            <Button x:Name="btnEditTask" Style="{StaticResource WarningButton}" Click="btnEditTask_Click">Edit Task</Button>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="4">
            <Button x:Name="btnBack" Style="{StaticResource BackButton}" Click="btnBack_Click">Back</Button>
        </Grid>
       
    </Grid>
</Page>
